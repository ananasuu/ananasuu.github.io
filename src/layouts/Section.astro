---
interface Props {
	id: string;
	title: string;
	class?: string;
}

const { id, title, class: className } = Astro.props;
---

<section id={id} class={className}>
	<header class="major">
		<h2>{title}</h2>
		<slot name="description" />
	</header>
	<slot />
</section>
